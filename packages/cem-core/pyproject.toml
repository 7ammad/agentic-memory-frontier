[project]
name = "cem-core"
version = "0.1.0"
description = "Causal Experience Memory kernel"
requires-python = ">=3.11"
dependencies = ["pydantic>=2"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
